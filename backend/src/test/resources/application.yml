spring:
  datasource:
    url: jdbc:h2:mem:individual_research_test;MODE=MySQL;DATABASE_TO_LOWER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:

app:
  auth:
    secret: individual-research-test-secret
