server:
  port: 8000

spring:
  application:
    name: individual-research-backend
  datasource:
    url: ${DB_URL:jdbc:mysql://127.0.0.1:3307/individual_research?createDatabaseIfNotExist=true&serverTimezone=Asia/Seoul&useUnicode=true&characterEncoding=utf8}
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:}

app:
  auth:
    secret: dongguk-individual-research-dev-secret
